import { RUNTIME_SELF_RECURSION_ENDURANCE_VERSION } from '../constants/runtimeSelfRecursionEndurance';

console.log(`runtimeSelfRecursionEndurance: OK (v${RUNTIME_SELF_RECURSION_ENDURANCE_VERSION})`);
