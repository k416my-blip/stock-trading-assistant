/** 投資配分UIの表示モード — 判定ロジックには影響しない */
export type InvestmentDisplayMode = 'trust' | 'beginner' | 'pro';

export const INVESTMENT_DISPLAY_MODE_ORDER: InvestmentDisplayMode[] = ['trust', 'beginner', 'pro'];
